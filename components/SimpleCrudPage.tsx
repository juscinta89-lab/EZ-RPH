'use client'
import { useState } from 'react'
import { apiFetch } from '@/lib/firebase/request'
export function SimpleCrudPage({title,eyebrow,description,table,columns,addLabel='Tambah'}:{title:string;eyebrow:string;description:string;table:string;columns:string[];addLabel?:string}){const [items,setItems]=useState<any[]>([]);const [busy,setBusy]=useState(false);async function load(){setBusy(true);const r=await apiFetch(`/api/simple?table=${encodeURIComponent(table)}`);const j=await r.json();setItems(Array.isArray(j)?j:[]);setBusy(false)}return <div className="page"><div className="page-header"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="muted">{description}</p></div><button className="primary" onClick={load}>{busy?'Memuat...':addLabel}</button></div><div className="card"><div className="table-wrap"><table><thead><tr>{columns.map(c=><th key={c}>{c}</th>)}</tr></thead><tbody>{items.length?items.map((x,i)=><tr key={i}>{columns.map(c=><td key={c}>{x[c.toLowerCase().replace(/ /g,'_')]??'-'}</td>)}</tr>):<tr><td colSpan={columns.length} className="empty">Tiada data dipaparkan. Gunakan modul import atau masukkan data melalui database.</td></tr>}</tbody></table></div></div></div>}
