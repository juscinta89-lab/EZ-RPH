'use client'
import { useState } from 'react'
export default function AIPage(){const [instruction,setInstruction]=useState('');const [section,setSection]=useState('Aktiviti');const [currentText,setCurrentText]=useState('');const [result,setResult]=useState('');const [loading,setLoading]=useState(false);async function run(){setLoading(true);const r=await fetch('/api/ai/assistant',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({instruction,section,currentText})});const j=await r.json();setResult(j.text||j.error||'');setLoading(false)}return <div className="page"><div className="page-header"><div><p className="eyebrow">AI ASSISTANT</p><h1>Pembantu RPH</h1><p className="muted">Minta AI pendekkan, tambah KBAT, PAK21, pemulihan atau ubah tempoh.</p></div></div><div className="grid-2"><div className="card stack"><label>Bahagian<select value={section} onChange={e=>setSection(e.target.value)}><option>Objektif</option><option>Aktiviti</option><option>Pentaksiran</option><option>Refleksi</option></select></label><label>Arahan guru<textarea value={instruction} onChange={e=>setInstruction(e.target.value)} placeholder="Contoh: Buat aktiviti lebih sesuai untuk murid lemah."/></label><label>Teks semasa<textarea value={currentText} onChange={e=>setCurrentText(e.target.value)}/></label><button className="primary" onClick={run}>{loading?'Memproses...':'✨ Jalankan AI'}</button></div><div className="card"><h2>Cadangan AI</h2><div className="ai-output">{result||'Hasil AI akan dipaparkan di sini.'}</div></div></div></div>}
