'use client'
import { useState } from 'react'
const steps=['Profil Guru','Sekolah','Kelas','Subjek','Jadual Waktu','Takwim','DSKP','Buku Teks','Sedia Menjana']
export default function SetupPage(){const [step,setStep]=useState(0);return <div className="page"><div className="page-header"><div><p className="eyebrow">SETUP WIZARD</p><h1>Selamat Datang ke e-RPH AI</h1><p className="muted">Lengkapkan asas sistem sebelum generation.</p></div></div><div className="card"><div className="stats-grid">{steps.map((s,i)=><div className="stat" key={s} style={{opacity:i===step?1:.55}}><span>Step {i+1}</span><b>{i===step?'●':i<step?'✓':'○'}</b><div>{s}</div></div>)}</div><div className="section-head"><div><h2>{steps[step]}</h2><p className="muted">{step===8?'Semua asas telah disusun. Anda boleh mula menjana RPH.':'Sediakan maklumat bahagian ini. Anda boleh datang semula kemudian.'}</p></div><div className="button-row">{step>0&&<button className="secondary" onClick={()=>setStep(x=>x-1)}>Kembali</button>}<button className="primary" onClick={()=>setStep(x=>Math.min(8,x+1))}>{step===8?'Pergi ke Dashboard':'Seterusnya'}</button></div></div></div></div>}
