'use client'
import {useEffect,useState} from 'react'
import {onAuthStateChanged} from 'firebase/auth'
import {auth} from '@/lib/firebase/client'
export default function AdminPage(){const [email,setEmail]=useState('');useEffect(()=>onAuthStateChanged(auth,u=>setEmail(u?.email||'')),[]);return <div className="page"><div className="page-header"><div><p className="eyebrow">ADMIN PANEL</p><h1>Pentadbiran</h1><p className="muted">Urus pengguna, knowledge base dan statistik.</p></div></div><div className="stats-grid"><div className="stat"><span>Akaun semasa</span><b>{email||'-'}</b></div><div className="stat"><span>Database</span><b>Firestore</b></div><div className="stat"><span>AI Provider</span><b>{process.env.NEXT_PUBLIC_AI_PROVIDER||'OpenAI'}</b></div><div className="stat"><span>Authentication</span><b>Firebase</b></div></div><div className="grid-2"><div className="card"><h2>Pengurusan data</h2><p className="muted">Import DSKP, buku teks dan takwim yang sah.</p><a className="secondary button-link" href="/import">Buka Data Import</a></div><div className="card"><h2>Security</h2><p className="muted">Firestore Rules mengasingkan data mengikut akaun guru.</p></div></div></div>}
