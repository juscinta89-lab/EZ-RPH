'use client'
import { useState } from 'react'
export default function ImportPage(){const [table,setTable]=useState('standards');const [text,setText]=useState('');const [msg,setMsg]=useState('');async function submit(){const r=await fetch('/api/import',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({table,data:text})});const j=await r.json();setMsg(j.message||j.error||'Selesai')}return <div className="page"><div className="page-header"><div><p className="eyebrow">DATA IMPORT</p><h1>Import Knowledge Base</h1><p className="muted">Sokongan awal untuk data DSKP, buku teks dan takwim. Gunakan data rasmi yang anda mempunyai hak untuk gunakan.</p></div></div><div className="grid-2"><div className="card stack"><label>Jenis data<select value={table} onChange={e=>setTable(e.target.value)}><option value="standards">DSKP / Standards</option><option value="textbooks">Buku Teks</option><option value="academic_calendar">Takwim</option></select></label><label>CSV / JSON<input type="file" accept=".csv,.json,.txt" onChange={async e=>{const f=e.target.files?.[0];if(f)setText(await f.text())}}/></label><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Tampal CSV/JSON di sini untuk ujian atau preview."/><button className="primary" onClick={submit}>Import</button>{msg&&<div className="info-box">{msg}</div>}</div><div className="card"><h2>Peraturan sumber</h2><p className="muted">Sistem tidak mereka-reka standard, buku teks atau takwim rasmi. Data perlu disediakan/import oleh admin.</p></div></div></div>}
